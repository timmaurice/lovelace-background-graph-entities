# Dependencies
/node_modules

# Build output — built fresh by the release workflow and shipped as a release asset
dist/

# Lockfiles for other package managers
bun.lock
yarn.lock

.DS_Store

# Playwright
playwright-report/
test-results/
test/e2e/screenshots/

# Home Assistant test environment
docker-compose.ha.yml
ha-config/

# Home Assistant driven Playwright suite
playwright-report-ha/
test-results-ha/

# Local machine compose override (corporate CA, local paths)
docker-compose.override.yml
